const names = require('./04-names');
const sayHi = require('./05-utils');
const data = require('./06-alternative-flavor');

// since there is a function call in 07-mind-grenade.js, it will run the function
// if there wasnt a function call, i would need to asign it to a variable and then call it
require('./07-mind-grenade');


// sayHi("susan");
// sayHi(names.john);
// sayHi(names.peter);

// console.log(data);